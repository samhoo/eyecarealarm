# 护眼提醒工具（EyeCareAlarm）

一个护眼提醒工具（EyeCareAlarm） 应用，支持多语言、多平台（Windows / macOS）
跨平台（Windows / macOS）、多语言护眼提醒小工具。常驻系统托盘，每 20 分钟弹出一个 20% 透明全屏遮罩，提醒你「请看向至少6米(20英尺)外远处物体 20 秒」，并倒计时 20 秒，期间播放宁静放松的音效；倒计时结束自动退出提醒，程序回到托盘。提醒期间提供「紧急退出」按钮。

技术栈：**Tauri v2（Rust）+ React + TypeScript + i18next**。

## 安装
mac .dmg安装包标准即可（需支持intel 和m系列）
windows: 安装包,缺省安装到程序目录中


## 运行
1）如果无实例在运行，正常启动，并提示："护眼提醒已启动"。悬停3秒后自动关闭
程序运行在后台，只在系统托盘(win)/菜单栏(mac)中出现，不在任务栏的任务按钮(win)/程序坞(mac)中出现
2）如果已有实例在运行，不重复启动新的实例，提示："护眼提醒已启动"。悬停3秒后自动关闭

## 控制面板
点击(左键和右键都可以)系统托盘(win)/菜单栏(mac)的本APP图标，进入控制面板
界面描述见控制面板设计

## 定时触发
显示覆盖全屏的遮罩，遮罩上有：
    文字提示区
    “退出(Esc) x秒”按钮，x秒为倒计时时间

遮罩分两阶段显示27秒，然后自动关闭。
    阶段一（预热阶段）：5秒，这个阶段，还能正常的输入，点击
        遮罩透明度由100%全透明逐渐变为控制面板中设置的透明度（缺省20%）
        显示文字：“眼睛需要休息啦~” 2秒渐显，2秒正常显示，1秒渐隐
        
    阶段二（正式阶段）：22秒
        5- 7s: 显示文字：“请看向至少6米(20英尺)外远处物体{{休息时长}}秒”。 2秒渐显，其中第5秒是和上一个阶段的最后1秒重合的。
        8-27s: 文字全显后，播放音乐{{休息时长}}秒，音效自动实现2秒渐隐收尾。 这段时间内，所有输入，点击都被屏蔽
          28s: 遮罩退出，恢复输入，点击。
点击"退出"按钮，或ESC键立即退出遮罩.
遮罩的退出采取渐隐(1秒)退出，即从当前透明度渐变为100%透明度。

## 更多设计
### 控制面板设计
控制面板界面示意BEGIN

x分x秒后眼睛休息一下，今日眼睛已放松X次

提醒休息眼睛                       [0/1]
---------------------------------------
立即提醒
提醒间隔                        [  ]分钟
休息时长                        [  ]秒
遮罩透明度             xx% <-----o----->
音效          [v                     v]
音量            xx% <-----o------>
语言                     [v          v]
开机自启动                         [0/1]
---------------------------------------
更多护眼知识
关于EyeCareAlarm version 0.1.23
退出EyeCareAlarm

控制面板界面示意END

#### 图例说明
[0/1] :开关选项, 
[  ] :输入框,带上下箭头加减数字
 <-----o-----> : 滑动条，滑动条和百分比数字联动，滑点及其左边滑轨是绿色，右边是灰色
 [v                 v] : 单选框
 ---------------------------------- ：分割线

#### 控制逻辑
x分x秒后眼睛休息一下 - 灰色
关闭“提示休息眼睛”，折叠除了“提醒休息眼睛”、"退出"的所有菜单项，位置要求：“提示休息眼睛” 的绝对位置不改变。
音效：
选项为：无音效；程序目录中sound文件夹中的音频文件（选项显示无后缀文件名, 如果超过选项框的长度，截断，选项停留3秒，则显示完整无后缀文件名）；+我的音效（选择后弹出选择音频文件的对话框，选择后将该音频文件拷贝到用户数据目录中，并更新选项）。选择后，按下面设置音量的大小播放，播放选中的音效5秒
音量：设置好音量后，按音量大小播放设置的音效，如无设置音效，播放第一个选项音效(默认音效)
今日眼睛已放松X次 - 完整结束才算一次

## 其它特性
多屏支持：当用户外接显示屏时，程序会自动适配多屏，遮罩覆盖所有屏幕
全屏勿扰：如果用户处于全屏模式（如播放PPT，全屏会议电话，打游戏等），禁止触发，本次提醒跳过重置倒计时


## 考虑可扩展点<暂不实现>
1. 更好的遮罩的效果
1. 音效：音效选取，AI生成音效
1. 护眼统计（日热力图）
1. 护眼成就系统
1. 提示文字自己定义
1. 护眼知识：形式：图文并茂。时机：安装或启动程序时，菜单项中主动点击
1. 捐赠